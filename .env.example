# Cloudflare D1 Database
# This is automatically bound in Cloudflare Pages environment
# For local development, use wrangler.toml configuration

# Optional: GitHub Personal Access Token
# Used for syncing repository information
# Create at: https://github.com/settings/tokens
# Required scopes: repo (for private repos) or public_repo (for public repos only)
GITHUB_TOKEN=your_github_token_here

# Optional: Admin Password
# Simple password protection for the application
ADMIN_PASSWORD=your_admin_password_here

# Optional: Cron Secret
# Secret key for authenticating scheduled tasks
CRON_SECRET=your_cron_secret_here
